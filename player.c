#include <stdio.h>
#include <assert.h>
#include <stdlib.h>
#include <string.h>
#include "linkedlist.h"
#include "map.h"

void move(Player*p, char next){
	int current=p->room;
	if(next in g->rms[current]){
		p->room=next
	else:
		error("invalid")
	}
}